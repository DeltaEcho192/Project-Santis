tr
	td=item.item_name
	td=item.category
	td
		button(
			class="btn btn-danger"
			hx-get=`/item/${item.item_id}/edit`
			) Edit
	td
		button(
			id="showButton"
			hx-get="uikit-modal.html"
			hx-target="#modals-here"
			hx-swap="inner-html"
			class="uk-button uk-button-primary"
			_="on htmx:afterOnLoad wait 10ms then add .uk-open to #modal"
			) Open 

